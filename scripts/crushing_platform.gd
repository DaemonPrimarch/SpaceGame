extends Node2D

export var forward_velocity = Vector2()
export var forward_acceleration = Vector2()

export var backward_velocity = Vector2()
export var backward_acceleration = Vector2()

func _process(delta):
	# Called every frame. Delta is time since last frame.
	# Update game logic here.
	pass
